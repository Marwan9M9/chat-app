export class Friend {
    public name: string;
    public img: string;
    public msg: string[];

    constructor(name: string, img: string, msg: string[]) {
        this.name = name;
        this.img = img;
        this.msg = msg;
    }
}
